import Layout from "./Layout/Layout";
import MainContainer from "./Main/MainContainer";

const MainPage = () => {
	return (
		<>
			<Layout>
				<MainContainer/>
			</Layout>
		</>
	);
}

export default MainPage;